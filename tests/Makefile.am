
TESTS_ENVIRONMENT   = TERM='' \
	COMPILE='$(COMPILE)' CFLAGS='$(CFLAGS)' LDFLAGS='$(LDFLAGS)' \
	SHELL=$(SHELL) \
	top_builddir='$(top_builddir)' top_srcdir='$(top_srcdir)'

TESTS               = complexity.test
EXTRA_DIST          = $(TESTS) sample.c
